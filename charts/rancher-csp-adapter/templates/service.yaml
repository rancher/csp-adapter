kind: Service
apiVersion: v1
metadata:
  name: {{ .Chart.Name }}
  namespace: cattle-csp-system
spec:
  ports:
  - port: 443
    targetPort: 9443
    protocol: TCP
    name: https
  selector:
    app: {{ .Chart.Name }}
