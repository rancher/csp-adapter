{{- if .Values.aws -}}
    {{- if and .Values.aws.accountNumber .Values.aws.roleName -}}
    {{- else -}}
    {{ fail "AWS account number and role name are required" }}
    {{- end -}}
{{- else -}}
{{ fail "AWS account number and role name are required" }}
{{- end -}}